#pragma once


namespace easing
{
	extern Vector4 lerp( const Vector4& v1, const Vector4& v2, float alpha );
	extern Vector3 lerp( const Vector3& v1, const Vector3& v2, float alpha );
	extern Vector2 lerp( const Vector2& v1, const Vector2& v2, float alpha );
}